startup --batch

build --copt="-std=c++20"
build --incompatible_enable_cc_toolchain_resolution
build --host_platform=@toolchain//:linux_x86_64 --platforms=@toolchain//:linux_x86_64

build:arm64 --platforms=@toolchain//:linux_arm64

build:bear --action_env=LD_PRELOAD=${LD_PRELOAD}
build:bear --spawn_strategy=local
