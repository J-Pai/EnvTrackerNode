startup --batch

build --action_env=LD_PRELOAD=${LD_PRELOAD}
build --spawn_strategy=local
build --copt="-std=c++20"
build --host_platform=@toolchain//:linux_x86_64 --platforms=@toolchain//:linux_x86_64

build:arm64 --platforms=@toolchain//:linux_aarch64
